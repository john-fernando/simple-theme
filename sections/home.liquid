
<section class="w-full min-h-screen flex flex-col items-center justify-center text-center bg-gray-100 overflow-auto">
    <h1 class="text-4xl font-bold">ようこそ！</h1>
    <p class="mt-4 text-lg">シンプルなオンラインストアへ</p>

    <div class="mt-6 flex gap-4">
        <a
            href="/collections/all"
            class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded"
            aria-label="商品一覧ページに移動"
        >
            商品一覧を見る
        </a>
        <a
            href="/account"
            class="bg-gray-500 hover:bg-gray-600 text-white px-6 py-3 rounded"
            aria-label="ユーザーページに移動"
        >
            ユーザーページ
        </a>
    </div>

    <div class="lg:text-emerald-500 text-gray-500 md:text-amber-300 xl:text-blue-600">これはテスト</div>
</section>

{% schema %}
    {
        "name": "Home",
        "presets": [{ "name": "Home" }]
    }
{% endschema %}
