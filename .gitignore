# Shopify の設定ファイル（環境依存のため）
shopify.theme.toml

# Node.js 関連ファイル（依存関係は package.json で管理）
node_modules/
package-lock.json

# macOS の不要ファイル（Finderが生成するメタデータ）
.DS_Store
._*
.DS_Store?
.Spotlight-V100
.Trashes

# Windows の不要ファイル（エクスプローラーのキャッシュ）
ehthumbs.db
Thumbs.db

# VSCode 設定ファイル（個人環境による差がある）
.vscode/

# Shopify のローカル設定フォルダ（Git には不要）
.shopify/

# Tailwind CSS のビルド結果（Shopify にアップロード不要）
assets/tailwind.output.css

config/settings_data.json

